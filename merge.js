function mergeSort(a){
    if (a.length <= 1) return a;
}

//check if array already sorted
    //return array if true

    
//find the smallest number
    //loop over the array based on its length to find smallest one
    //remove smallest number from original array
    //save smallest number on variable
    //feed reduced array on function once more
//push removed number into first position of final array



//returned sorted array
